@page "{hash?}"
@functions
{
    public IActionResult OnGet()
    {
        var hash = RouteData.Values["hash"]?.ToString();
        return Redirect(hash == null ? "/replays/list" : $"/replays/map/{hash}");
    }
}
